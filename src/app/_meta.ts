import type {MetaRecord} from 'nextra'

const meta: MetaRecord = {
    index: {
        title: 'Home',
        type: 'page',
    },
    protocol: {
        title: 'Protocol',
        type: 'page'
    }
}

export default meta